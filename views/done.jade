extends layout

block content
  h1= title
  p= content
  button(id='send-button' onclick='hello()') Send
  table(border="1")
    - for (i = 0; i < items.length; i++)
      tr
        td=items[i]['date']
        td=items[i]['count']
